image:
  repository: ghcr.io/neodjazz/ai-gateway-auth
  # Defaults to Chart.appVersion when empty.
  tag: ""
  pullPolicy: IfNotPresent

devMode:
  enabled: false
  sourceHostPath: /mnt/c/tmp/ai-gateway
  image:
    repository: golang
    tag: 1.26.7-alpine
    pullPolicy: IfNotPresent

auth:
  replicaCount: 1
  port: 8082
  jwt:
    # Legacy HS256 secret. Ignored when jwksUrl is configured.
    secret: dev-jwt-secret
    issuer: ai-gateway
    audience: ai-gateway
    # Direct JWKS endpoint for RS256/ES256 OIDC access tokens.
    jwksUrl: ""
    jwksCacheTtlSeconds: 300
    clockSkewSeconds: 30
    userIdClaim: sub
    teamIdClaim: team_id
    rolesClaim: roles
  postgres:
    enabled: true
    dsn: postgres://ai_gateway:ai_gateway@ai-gateway-postgres:5432/ai_gateway?sslmode=disable
  keyHashSecret: dev-auth-key-hash-secret
  # Must match gateway.management.sharedSecret.
  managementSharedSecret: dev-management-shared-secret
  staticKeyFallbackEnabled: true
  demoKeysEnabled: true
  # Production values should be supplied from an encrypted values source.
  virtualKeys: []
  # - token: replace-me
  #   user_id: user-1
  #   team_id: team-1
  #   roles: [developer]
  #   allowed_models: ["gpt-5.*"]
  #   allowed_tools: ["mcp.weather.*", "mcp:weather-prod@https://mcp.example.test"]
  #   rate_limit_rpm: 60
  #   rate_limit_tpm: 100000

resources: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 100
  runAsGroup: 101

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: false
  capabilities:
    drop:
      - ALL
